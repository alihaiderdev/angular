import { EmailValidtorDirective } from './email-validtor.directive';

describe('EmailValidtorDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailValidtorDirective();
    expect(directive).toBeTruthy();
  });
});
